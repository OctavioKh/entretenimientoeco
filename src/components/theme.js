import { createTheme } from "@mui/material/styles";


const theme = createTheme({
    typography: { fontFamily: "Britti Bold, Britti Medium, Britti Thin" },

    bgcolor: "#a5c4c6",
    palette: {
      primary: {
        main: "#a5c4c6",
      },
      secondary: {
        main: "#856c8a",
      },
    },
  });
  

  export default theme;